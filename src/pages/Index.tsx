import { Layout } from "@/components/Layout";
import { CodeEditor } from "@/components/Editor";

const Index = () => {
  return (
    <Layout>
      <CodeEditor />
    </Layout>
  );
};

export default Index;