import { Layout } from "@/components/Layout";
import { Editor } from "@/components/Editor";

const Index = () => {
  return (
    <Layout>
      <Editor />
    </Layout>
  );
};

export default Index;