export type User = {
    createdAt: Date
    deletedAt: Date | null
    email: string
    emailVerified: boolean
    emailVerifiedAt: boolean
    firstName: string
    lastName: string
    id: string
    updatedAt: string
}